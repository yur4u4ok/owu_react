const baseURL = 'http://owu.linkpc.net/carsAPI/v2'

const   urls = {
    cars: '/cars',
    auth: {
        login: '/auth',
        refresh: '/auth/refresh'
    },
    users: '/users'
}

export{
    baseURL, urls
}
